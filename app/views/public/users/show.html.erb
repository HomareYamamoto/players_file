<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <h2 class="mt-3">Profile</h2>
      <table class='table'>
        <tr>
      	  <th>name</th>
      	  <th><%= @user.name %></th>
        </tr>
        <tr>
          <th>follows</th>
          <th></th>
          <!--<th><%#= link_to "#{@user.followings.count}", publi_user_followings_path(@user.id) %></th>-->
          <!--フォロー機能実装時にリンク追加-->
        </tr>
        <tr>
          <th>followers</th>
          <th></th>
          <!--<th><%#= link_to "#{@user.followers.count}", public_user_followers_path(@user.id) %></th>-->
          <!--フォロー機能実装時にリンク追加-->
        </tr>
      </table>
      <div>
        <% if @user == current_user %>
          <%= link_to "Profile edit", edit_public_user_path(current_user.id), class: 'btn btn-success' %>
        <% else %>
        <% end %>
        <!--<%# if current_user.following?(user) %>-->
        <!--    <%#= link_to  "フォロー外す", user_relationship_path(user.id), class: "btn btn-secondary", method: :delete %>-->
        <!--  <%# else %>-->
        <!--    <%#= link_to "フォローする", user_relationship_path(user.id), class: 'btn btn-success', method: :post %>-->
        <!--  <#% end %>-->
        <!--フォロー機能実装時に追加-->
      </div>
    </div>
    <div class='col-md-8 offset-md-1'>
      <h2 class="mt-3">PLAYERS</h2>
      <%= render 'public/users/index',post_players: @post_players %>
      <!--共通テンプレート作成し、投稿者の投稿のみを一覧表示-->
    </div>
  </div>
</div>